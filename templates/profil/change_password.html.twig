{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-container">
        <h1>🔐 Modifier mon mot de passe</h1>

        {{ form_start(form) }}
            {{ form_row(form.plainPassword) }}

            <button class="btn">💾 Sauvegarder</button>
        {{ form_end(form) }}

        <br>
        <a href="{{ path('app_profil') }}">⬅️ Retour à mon compte</a>
    </div>
{% endblock %}
