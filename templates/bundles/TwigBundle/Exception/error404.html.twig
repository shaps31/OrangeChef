{% extends 'base.html.twig' %}
{% block title %}Page introuvable (404) — Orange Chef{% endblock %}

{% block body %}
    <div class="container" style="max-width:880px;margin:0 auto">
        <section class="surface rounded shadow" style="padding:28px">
            <div style="display:flex;gap:20px;align-items:center;flex-wrap:wrap">
                <div style="font-size:52px">🧭</div>
                <div>
                    <h1 style="margin:0 0 6px">😕 Oups, page introuvable</h1>
                    <p class="muted">Le lien est peut-être cassé ou la page a été déplacée.</p>
                    <p class="muted" style="margin:0 0 12px">
                        On ne trouve pas cette page. Essayez la recherche ou retournez à l’accueil.
                    </p>
                    <div style="display:flex;gap:10px;flex-wrap:wrap">
                        <a class="btn btn-primary" href="{{ path('app_home') }}">🏠 Accueil</a>
                        <a class="btn btn-secondary" href="{{ path('app_recipe_index') }}">🍊 Voir les recettes</a>
                        <a class="btn btn-outline" href="{{ path('app_contact') }}">✉️ Nous contacter</a>
                    </div>
                </div>
            </div>

            <form action="{{ path('app_recipe_index') }}" method="get" style="margin-top:16px;display:flex;gap:8px;flex-wrap:wrap">
                <input class="form-control" type="text" name="q" placeholder="Rechercher une recette…">
                <button class="btn btn-primary">Rechercher</button>
            </form>
        </section>
    </div>
{% endblock %}
